platform:
  - x64

install:
  - ps: Install-Product node 7 x64
  - npm install -g electron-forge
  - npm install

build_script:
  - npm run make
  - npm run publish

test: off

# environment:
#   GH_TOKEN:
#     secure:
